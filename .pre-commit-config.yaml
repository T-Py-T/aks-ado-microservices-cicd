repos:
  - repo: local
    hooks:
      - id: repository-policy
        name: repository policy
        entry: python3 -m unittest tests.test_repository_policy
        language: system
        pass_filenames: false
